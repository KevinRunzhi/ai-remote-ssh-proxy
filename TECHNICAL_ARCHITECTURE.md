# 技术架构：首次真实环境端到端验证

> 依据：[首版 PRD](PRD.md)。状态：验证方案，尚未执行真实端到端测试。日期：2026-09-06。
> 本文只设计打通首条路径所需的验证脚本，不定义正式发布架构，也不恢复旧规格。

## 1. 本次要证明什么

在一台 Windows 和一个受信任的 Linux 目标上，用户运行脚本完成必要配置，重新连接 VS Code Remote SSH，远程 Codex 能通过 Windows 的本地代理完成一次用户发起的请求。

成功必须同时有三类证据：**隧道可达、实际 Codex 后端采用代理、真实请求通过这条代理路径完成**。远端终端里 curl 成功只能证明第一类。

只记录这一组实测版本的结论。当前样本已有直接 SSH 密码登录成功的记录，但 Node 调用 SSH 时认证交互阻塞；端到端尚未通过，详见 `docs/E2E_RUN.md`。本次传输修订仍待实测。

先做针对该样本的最小可撤销试验：维护者脚本预览并修改本文的两处配置，用户重连、发起请求，确认进程采用代理且路径证据成立，再撤销试验改动。试验保留第 5 节的必要写入与恢复保护，但不等待完整命令交互、内部接口和测试套件。通过后再把已验证操作整理为三个命令；失败则停在当前假设，不先完成整套工具。

## 2. 最小框架与依据

选择 **Node.js 脚本 + Windows 系统 OpenSSH + 远端 VS Code 设置**。

| 部件 | 选择及原因 |
| --- | --- |
| 本地入口 | JavaScript ESM，开发机已有的受支持 Node.js LTS；直接运行，无 TypeScript 编译、安装器或可执行文件打包 |
| SSH / SCP | 调用与 VS Code 相同的 `ssh.exe` 及同一套 OpenSSH 的 `scp.exe`，使用同一配置入口；必须支持现有密码登录，终端交互交给 OpenSSH |
| 配置编辑 | Node 内置文件能力；唯一运行依赖为 Microsoft `jsonc-parser`，局部编辑两个 JSONC 属性，保留其他内容与注释 [1] |
| 远端操作 | 短生命周期 SSH 命令执行固定 POSIX sh 操作；不安装 Node、Python 或远端服务 |
| 网络检查 | Windows `curl.exe`、Linux `curl`；显式指定代理、目标、超时和 TLS 校验 |
| 持续转发 | 用户重连后的 VS Code SSH 连接承载，脚本退出后不留自己的后台隧道 |

开发机需要 Node.js 是本次验证的前提，不是已经决定的正式用户安装要求。

**选择依据与尚未证明的部分：**

- Microsoft 支持独立的远端设置；关闭 `http.useLocalProxyConfiguration` 后，`http.proxy` 可以在本地与远端分别配置。[2][3]
- 本机已安装的 Codex 扩展 `26.818.41705` 中，`out/extension.js` 第 673 行的调用链为：读取 `http.proxy` → 构造 `HTTP_PROXY` / `HTTPS_PROXY` → 合并到 `spawn` 的环境 → 启动 `codex app-server`。这是本机包的静态证据，**还不是 Linux 目标包或真实运行的证据**。
- 执行前核对目标远端包有相同调用行为，重连后核对实际后端进程。若不成立，本轮停止在此处，不自动改用其他注入方式。

候选取舍：远端设置改动最少，作为本轮主路径；Shell 启动文件会扩大作用域且不能证明扩展进程继承，不采用；通过 `chatgpt.cliExecutable` 包装启动属于官方标明的开发用途，也不采用。[4]

## 3. 运行链路

```mermaid
flowchart LR
    subgraph W[Windows]
        S[验证脚本]
        C[VS Code / OpenSSH]
        P[本地 HTTP 代理]
    end
    subgraph L[Linux]
        V[VS Code 远端设置]
        X[远端 Codex 扩展]
        B[Codex app-server]
        R[127.0.0.1:远端端口]
    end
    S -. 修改转发配置 .-> C
    S -. SCP 上传临时文件并由 SSH 校验替换 .-> V
    V -. 读取 http.proxy .-> X
    X -. 启动时传入代理环境 .-> B
    B --> R
    R -->|VS Code SSH 反向转发| C
    C --> P
    P --> O[OpenAI 服务]
```

虚线表示配置与启动关系，实线表示请求方向。本地与远端端口独立，例如本地 `7890`、远端 `17890`；示例值不替代实际输入。

## 4. 只修改两处配置

### Windows：现有 SSH 目标块

确定 VS Code 实际使用的 SSH 配置文件，在目标唯一的字面 `Host` 块内添加带标记的转发指令。示例为增量，其他连接配置原样保留：

```sshconfig
Host example-linux
    # 原有 HostName、User、IdentityFile 等保留
    # BEGIN codex-proxy-e2e
    RemoteForward 127.0.0.1:17890 127.0.0.1:7890
    # END codex-proxy-e2e
```

本次验证只处理普通文件中唯一、单别名的目标块；遇到 Include、Match、重复目标块或链接文件停止，记录为本轮未覆盖。用 `ssh -G` 比较变更前后，允许的有效差异只有目标转发；不重写认证、跳板或用户已有转发。已有等价转发仅复用，不标记为本工具所有。[5]

不新增持久的 `ExitOnForwardFailure=yes`，避免重复会话竞争端口时影响正常登录；若用户已有该设置且导致重连失败，报告冲突。端口已占用且不能证明是已知等价配置时，停止并由用户选择其他端口，不终止占用进程。

端口检查分支：首次新增转发要求候选端口空闲；已有配置或重复执行时，先核对记录（如有）、有效转发映射和配置内容，再独立检查端点。已知等价配置对应的回环端点可用则复用，不重复写入；配置一致但无监听则提示重连。来源不明或映射不符的占用仍停止。重复执行保留首次恢复信息，不把当前已应用值覆盖成“修改前状态”。端点可用不代表已证明其进程所有权，也不授权删除用户配置。

### Linux：VS Code Remote settings

通过目标窗口的 **Preferences: Open Remote Settings (JSON)** 确认实际设置文件路径。通常是 `~/.vscode-server/data/Machine/settings.json`，但以当前安装实际路径为准，不猜测自定义目录。定位文件可由维护者完成，配置内容由脚本修改。[3][6]

```json
{
  "http.useLocalProxyConfiguration": false,
  "http.proxy": "http://127.0.0.1:17890"
}
```

这是两个属性的增量，不是整文件覆盖模板。文件缺失时创建；存在时校验 JSONC 和重复目标键，依次计算并应用局部编辑。缺失或等价值可以处理，已有不同的用户值作为冲突停止。[1]

影响范围是**该远端用户的 VS Code 设置**，可能影响其他遵循代理设置的扩展；预览时必须说明。它不会给所有终端设置全局代理。本轮不改 `.bashrc`、系统环境、Codex `config.toml` 或登录文件，也不关闭 TLS 验证。已有关闭 TLS、代理认证或代理绕过配置导致证据不可信时，先报告阻塞。

## 5. 脚本组织与写入保护

以下为计划文件，不代表当前已有实现：

```text
scripts/e2e.mjs          参数、中文预览、确认、configure / verify / remove 顺序
scripts/ssh.mjs          ssh/scp/curl 进程、终端交互、超时、固定远端读写与检查
scripts/config.mjs       SSH 片段、JSONC 编辑、本次恢复信息
```

三个文件都是普通函数，不建立插件接口或事务框架。`configure` 完成写入后返回“需要重连”；`verify` 独立检查，不创建转发；`remove` 移除或恢复本次拥有的配置。

该文件划分用于样本试验通过后的整理；试验脚本可先集中在 `scripts/e2e.mjs`，仅服务已确认的目标和两处增量，不提前实现完整 CLI。

参数包括 `--host`、`--local-proxy`、`--remote-port`；维护者还可指定实际 `--ssh-config` 与 `--remote-settings`。Host 限定字母数字开头的字母、数字、点、短横线和下划线，端口限定 1024–65535，本地代理仅接受无认证的回环 HTTP URL。

**密码登录是首版必需能力。** 不要求用户改用公钥，不修改服务器密码、authorized_keys 或 sshd 配置。密码由 OpenSSH 原生提示直接接收，Node 不读取、保存或代填；不使用 AskPass 或密码参数/环境变量。首版允许每个 SSH/SCP 连接分别提示密码，合并同阶段只读检查即可，不引入连接复用或后台认证服务。

本地用 `spawn(executable, args, { shell: false })`。先在真实 PowerShell 对照直接 SSH 与最小 Node 探针：全继承终端通道成功后，仅把 stdout 改为管道，验证 `stdio: ['inherit', 'pipe', 'inherit']` 能同时支持密码输入与结果解析。[7] 这是待实测的方案，不能用本地进程模拟代替。认证探针只执行固定标记，不读取或修改用户配置；失败时停在此处，不先重构完整工具。

SSH stdin 留给原生交互，不再传递操作数据；stderr 原生提示直接显示但不被工具捕获或记录，stdout 在内存解析，不展示完整配置。Node 的交互询问结束并释放终端后才启动子进程；没有可用终端且需要认证时提示改在 PowerShell 运行。认证等待上限暂定 120 秒，启动前说明；收到远端就绪标记后再启动操作超时，最终成功需完整结果和退出码 0。取消或超时结束本次子进程，写入结果不明时走恢复流程。curl 等非交互调用仍使用管道，不能全局继承所有进程输出。

远端命令仍会经过 Shell。短小固定脚本与校验后的非秘密参数编码后放入命令参数，使用固定解码执行模板并验证特殊字符边界；Base64 只解决传输编码，不保护秘密。读取文件通过捕获的 stdout 返回，完整配置不放入命令参数。写入内容通过 SCP 上传到本次私有临时目录，由固定 SSH 脚本校验并替换。使用目标客户端默认传输方式，实施前确认服务器支持（通常为 SFTP），不自动切换协议或安装服务；先用无敏感内容样本验证传输、摘要和清理，再修改真实配置。

一次只允许一个活动验证记录，存于 `%LOCALAPPDATA%/codex-remote-ssh-proxy-e2e/`，限制为当前用户可访问。只保存修改前后的摘要、工具插入片段、两个设置原值或缺失状态及步骤结果；不保存完整设置文件、原始环境或 SSH 日志。相关原值含秘密时拒绝修改。

两处写入前都检查可恢复条件并保存必要信息；每处采用同目录临时文件、保留权限、校验原摘要、替换后复读。新远端私有文件权限为 `0600`。这是有限的安全替换，不宣称跨机器原子性；验证期间用户避免并行编辑相关文件。

SCP 上传的完整配置仅作短期传输：本地暂存限制为当前用户访问，远端暂存目录 `0700`、文件 `0600`。上传后核对上传内容摘要及当前目标摘要，再通过目标同目录临时文件替换，保留原文件权限；暂存内容不作为长期备份。成功或失败均清理本次暂存，断线无法清理时报告本次残留路径，后续先核对状态再恢复；不清理未知文件。配置预览与确认仍在上传真实配置和正式写入之前。

应用失败时逆序尝试恢复已改内容；断线导致结果未知时，重连后先比对修改前后摘要。摘要不符则保留记录并停止，提供涉及路径和人工恢复说明。移除时只动本工具所有的内容，等价用户配置保持原样；新建的设置文件仅在确认没有其他内容时删除。

## 6. 一次真实验证的执行顺序

样本试验先按下表走通配置、请求及撤销，检查操作可由维护者运行固定命令完成；重复配置在试验中检查增量为空。随后整理 configure/verify/remove 并复验同一流程，才计入首次完整 E2E 验收。

| 步骤 | 操作 | 通过证据 / 停止条件 |
| --- | --- | --- |
| 0. 固定样本 | 记录 Windows、Node、OpenSSH、VS Code、Remote SSH 和远端 Codex 扩展版本；确认 Linux 目标及实际配置路径 | 用户已有正常 SSH、远端 Codex 已登录；用 Running Extensions 确认 Codex 在远端运行；缺少远端 curl、sh 或监听检查工具则先停止 |
| 0a. 密码交互 | 同一目标下对照直接 SSH、Node 全继承、Node 仅捕获 stdout；只执行固定成功标记 | 原生提示可见、输入不回显、Node 收到完整标记且退出码为 0；取消能结束。未通过不进入传输改造 |
| 0b. 临时传输 | 确认 SCP 传输能力；仅在私有临时目录传输无敏感内容样本 | 摘要一致、空格路径可用、临时内容清理完成；不改用户配置，不以模拟测试替代 |
| 1. 记录基线 | 不改配置，检查本地代理、SSH、远端候选端口和目标包的代理启动行为 | 本地经代理 HTTPS 请求成功；首次新增要求端口空闲，已有配置按第 4 节分支核对；冲突或版本行为不符则停止 |
| 2. 配置 | 展示两处增量和影响范围，确认后先写 SSH、再写远端设置，复读校验 | 输出“配置已写入，需要重连”，不显示“Codex 已可用”；写入失败按第 5 节恢复 |
| 3. 重连 | 用户保存工作并关闭连接该目标的远端窗口，通过同一 alias 重新连接，打开 Codex | 新 SSH 会话承载转发；确认 Codex 后端重新启动；旧进程继续复用则此步未通过，不默认 Kill Server |
| 4. 独立验证隧道 | 检查连接关闭所有转发和连接复用，从 Linux 显式通过远端代理发 HTTPS 请求 | curl 正常结束、目标 TLS 校验通过、收到目标 HTTP 状态；远端监听确为 127.0.0.1，无 wildcard 监听 |
| 5. 验证实际进程 | 定位此远端窗口的 Codex app-server，远端过滤后只返回代理变量匹配结果 | HTTP_PROXY / HTTPS_PROXY 对应远端端口；冲突的大小写代理值、NO_PROXY 绕过或无法确认进程时不判通过 |
| 6. 人工完成请求 | 结束自动网络探测，用户在该 Codex 窗口发送一条简短请求，例如“只回复 OK，不执行工具” | 收到新回复，并将目标 Codex 后端进程关联到远端代理端口连接；同时间、同域名的代理日志只作辅助，脚本不发送模型请求 |
| 7. 检查可撤销 | 重复 configure 应无新增改动；运行 remove，再由用户重连并复查 | 本次配置被移除、既有 SSH 仍可用、用户内容保留；如需继续使用，重新 configure 并重复步骤 3–6 |

**独立探测的约束：**

- 所有 SSH/SCP 操作使用 `ClearAllForwardings=yes`、`ControlMaster=no`、`ControlPath=none`，禁止请求转发；命令执行结束即关闭。两者使用同一配置入口，保留主机密钥检查。`ClearAllForwardings` 会清除配置和命令行中的转发，不能与“创建测试转发”混用。[5]
- curl 使用 `-q`（作为首个选项忽略默认配置）、显式 `--proxy`、`--noproxy ""`、连接超时 5 秒与总超时 15 秒；不使用 `-k`、不携带凭证、不用将 401 变成传输失败的 `--fail`。Windows 侧通过参数数组传入空字符串，避免 PowerShell 命令行转义歧义。[8]
- 基础探测目标为 `https://api.openai.com/v1/models`；仅记录退出码、目标 HTTP 状态与 TLS 结果，不输出响应正文。401 可证明此目标网络可达；407、连接失败和 TLS 错误不通过。其他有效目标响应按网络可达、服务状态另列，不能等同模型可用。
- 这个 API 探测不代表 ChatGPT 登录方式下 Codex 的所有实际服务地址或长连接都可用，步骤 6 不可省略。
- 用 `ss` 或该机器已确认可用的等效工具检查实际绑定地址。服务器 GatewayPorts 配置可能改变绑定行为，不能仅凭写入了 127.0.0.1 就宣称安全；发现非回环监听立即停止验证并指导用户断开本次连接。[9]
- 进程环境在远端按代理字段白名单过滤，只返回匹配、冲突或缺失，不传回完整 `/proc/.../environ`。人工请求前结束 curl 等主动探测，只保留必要的只读连接观察；在请求期间核对已确认的 Codex 后端 PID、其连接的 socket 与远端代理端口，结合新回复记录结果。本地代理日志仅作辅助，不能用同时间或同域名匹配替代进程关联。证据只保留进程关联结果、目标域名、端口和时间，不采集认证头或请求内容。

## 7. 失败时只处理当前阻塞

| 现象 | 本轮行动 |
| --- | --- |
| 本地代理或 SSH 不通 | 保持配置未改，修复前提后重试 |
| 来源不明的端口占用、配置冲突或复杂配置 | 停止并记录；已知等价转发正常占用按第 4 节复用，不增加自动迁移或通用解析框架 |
| 配置已写入但未重连 | 保留配置并提示重连，这是中间状态 |
| curl 成功但 Codex 环境不对 | 查实际远端包、设置作用域及后端是否重启，不改全局 Shell |
| 环境正确但用户请求失败 | 记录 Codex 错误类别；检查实际服务路径，不能宣布 E2E 成功 |
| verify 失败 | 只报告当前证据并给出重试或 remove；不把网络波动说成已自动恢复配置 |
| 写入或移除结果未知 | 保留本次恢复信息，核对状态后再继续，不覆盖用户后续修改 |

如果缺少进程关联或其他能明确排除直连的路径证据，只能记录“Codex 可用，代理归因未证实”，不计入样本试验或 E2E 通过。必要时由用户在可中断的测试窗口做短暂代理断开/恢复对照，重新发起请求并排除缓存及其他路径；仅有同时段日志仍不够。脚本不自动停止代理软件或影响其他会话。

## 8. 交付与结束条件

本轮实现的交付物只需要：可运行验证脚本、依赖锁文件、简短运行说明，以及一份脱敏实测记录。记录包含版本组合、两处变更摘要、各步骤结果、用户请求是否成功、代理路径证据与移除结果；秘密和完整用户配置不进入仓库。

**样本试验通过**只证明路径可行，是完整命令实现的前提；**首次 E2E 通过**要求整理后的命令按步骤 0–7 复验，有真实证据并标明版本组合。**首版完成**仍需补齐 PRD 中代理未启动、SSH 失败、端口冲突、写入失败等验收，不能用一次成功样本替代。先打通成功路径及安全撤销，再完善命令和失败用例；不提前做其他平台、协议或历史恢复系统。

## 9. 资料与静态证据

- [1] [Microsoft jsonc-parser](https://github.com/microsoft/node-jsonc-parser)：JSONC 局部修改；多个编辑结果须按各自文本版本顺序应用。
- [2] [VS Code 代理设置定义](https://github.com/microsoft/vscode/blob/main/src/vs/platform/request/common/request.ts#L184-L262)：独立远端代理开关和配置作用域。
- [3] [Remote SSH 的目标专属设置](https://code.visualstudio.com/docs/remote/ssh#_ssh-hostspecific-settings)：远端设置入口。
- [4] [OpenAI IDE 设置说明](https://developers.openai.com/codex/ide/settings/)：cliExecutable 为开发用途。
- [5] [OpenSSH ssh_config](https://man.openbsd.org/ssh_config)：转发、连接复用及检查选项；实测仍以 Windows 安装版本为准。
- [6] [VS Code 服务端设置路径](https://github.com/microsoft/vscode/blob/main/src/vs/server/node/serverEnvironmentService.ts#L245-L251)：userDataPath 下的 Machine/settings.json。
- [7] [Node.js child_process](https://nodejs.org/api/child_process.html)：不经本地 Shell 启动子进程。
- [8] [curl 手册](https://curl.se/docs/manpage.html)：代理、绕过列表、超时和状态输出。
- [9] [OpenSSH GatewayPorts](https://man.openbsd.org/sshd_config#GatewayPorts)：服务端绑定策略。

Codex 本机静态证据：`%USERPROFILE%/.vscode/extensions/openai.chatgpt-26.818.41705-win32-x64/out/extension.js`，第 673 行，SHA-256 为 `ECBC2FC452DDE64F9BE4BCD93928A18BD091F2D7F23DCB6EF756B4C239C2747D`。可搜索 `getConfiguration("http")`、`HTTP_PROXY`、`Spawning codex app-server` 复查调用链；不要依赖压缩后的函数名。目标 Linux 包尚未检查。公开 main 分支资料会变化，实测时记录目标安装版本与相关证据。
