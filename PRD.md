# 首版 PRD：Windows 远程 Codex 代理配置

> v0.1 范围冻结 · 2026-09-06。本文件是首版需求的唯一依据，新增需求进入后续版本。

## 版本导航

- 本文保留 v0.1 冻结需求，作为已交付 CLI 的验收基线。
- [v0.2 PRD：VS Code 插件与引导使用](docs/PRD_V0.2.md)：2026-09-08 讨论稿，插件方向已确认，详细范围待评审；不代表插件已实现或通过验证。
- v0.2 的新增与变更需求以该版本 PRD 为依据；未明确变更的安全和配置保护要求继续沿用本文与 SECURITY.md。现有 TECHNICAL_ARCHITECTURE.md、SPEC.md 仍描述 v0.1，不能据此认定插件架构已验证。

## 目标与前提
提供 Windows 本地命令行引导工具，将本机已有代理通过 SSH 提供给远程 Linux 上的 Codex，让 VS Code Remote SSH 中的 Codex 可以联网使用，用户无需手工编辑配置。

用户已有可用的 Windows OpenSSH、VS Code Remote SSH 连接、本地无认证 HTTP 代理；远端为受信任的 Linux，已安装并登录 Codex。每次配置一个目标。

## 最小流程
1. 输入已有 SSH Host 别名、本地代理地址和端口；远端端口提供默认值并允许修改。
2. 检查 SSH、本地代理和已有配置；冲突或不支持的环境停止并说明原因。
3. 展示本地与远端变更，用户确认后自动配置反向转发及远程 Codex 所需代理环境。
4. 给出准确、简短的 VS Code Remote SSH 重连步骤，由用户完成重连。
5. 再次检查远端代理与目标服务网络路径，由用户确认远程 Codex 实际可用。

## 必须具备
- 支持现有 SSH 密码登录，密码由用户在 OpenSSH 提示中输入；不要求改成公钥登录，不更改服务器密码或认证配置，不由工具读取、保存或代填密码。首版允许不同连接分别提示输入密码。
- 自动完成必要配置，只支持验证过的 VS Code 远程 Codex 启动路径；两端端口可以不同。本地代理限定回环地址，远端入口限定 127.0.0.1。
- 重复运行不重复写入；等价配置复用，已知转发正常占用端口不算冲突，来源不明的占用停止，不自动接管或覆盖。
- 中文分层报告本地代理、SSH、远端代理和目标服务结果，区分检查通过、需要重连和失败，说明下一步动作。
- 修改前保留必要恢复信息；失败时尝试恢复本次修改，无法确认恢复时列明残留位置及人工恢复步骤。
- 提供移除本工具所加配置的入口；用户后续修改导致无法安全移除时停止。
- 不读取或修改 Codex 登录凭证，不记录密码、Token 或密钥。

## 验收标准
- 在一套真实 Windows → Linux → VS Code 远程 Codex 环境中，从未配置代理到实际使用成功，无需手工编辑文件。
- 重连后独立检查代理入口；验证连接不能创建自己正在验证的转发，临时连接成功不等于当前会话可用。
- 网络检查指定目标 HTTPS 地址并验证 TLS；目标服务的预期 HTTP 响应（包括无凭证探测的 401）可证明网络可达，代理自身错误不算通过。
- 网络可达不等于 Codex 可用；最终由用户实际操作确认，并取得能关联目标 Codex 进程的代理路径证据，单纯同时段的代理日志不足以证明。工具不自动发送模型请求。
- 重复配置、两端端口不同、代理未启动、SSH 失败、端口冲突、写入失败及移除配置均有可验证结果，并保留用户原有配置。

## 首版不做
macOS/Linux 本地支持、Claude 或其他工具、代理自动发现与管理、SSH 账号或服务器配置管理、全局终端代理、批量操作、后台保活、自动操作 VS Code、图形界面、通用适配器、稳定机器协议、历史事务管理、自动更新和多平台发布体系。

## 实现边界
先在目标环境验证代理进入实际 Codex 进程的配置位置和重连步骤，再自动化已验证路径，不能用交互式 Shell 联网代替。语言、模块和打包方式按最小实现决定，不预设架构。远端回环端口可被同机其他进程访问，首版不支持不受信任的共享服务器。
