# Codex Remote SSH Proxy PRD

> 状态：初稿 0.1  
> 本文只定义产品要解决的问题和首版范围，不预先规定技术架构。

## 1. 产品概述

为通过 VS Code Remote SSH 使用远程 Codex 的用户，提供一个引导式的一键配置工具。

当用户的本地电脑已经有可用代理、并且已经能够 SSH 登录远程 Linux 服务器时，工具帮助用户完成本地代理与远程 Codex 之间的连接、配置和验证，使用户不再需要手工编辑多个配置文件、反复重连或使用命令逐层排查。

“一键”不是完全没有交互。用户仍可能需要选择 SSH 主机、确认代理地址、完成 SSH 认证，以及按照提示重新连接 VS Code；但不需要理解 `RemoteForward`、代理环境变量或手工执行验证命令。

## 2. 问题背景

Codex 运行在 VS Code 的远程扩展环境中时，使用的是远程服务器的网络，而不是用户本地电脑的网络。即使本地 Clash Verge 等代理可以正常访问 OpenAI，远程 Codex 也不一定能够访问。

用户目前需要自行完成一组容易出错的步骤：

1. 找到本地代理的真实地址和端口。
2. 在正确的 SSH Host 配置中添加反向端口转发。
3. 区分本地代理端口和远程监听端口。
4. 让远程 Codex 或 VS Code 扩展环境使用远程代理地址。
5. 断开旧的 Remote SSH 会话并重新连接，使新配置生效。
6. 分别验证本地代理、SSH 隧道、远程网络和 Codex 状态。

任意一步遗漏或配置错误，Codex 都可能持续显示网络重连。用户也很难判断问题究竟发生在哪一层。

## 3. 目标用户

首版面向同时满足以下条件的个人开发者：

- 在本地 macOS 电脑上使用 VS Code；
- 通过 VS Code Remote SSH 连接远程 Linux 服务器；
- 在远程环境中使用 Codex；
- 本地已经运行可用的 HTTP 代理，例如 Clash Verge；
- 已经能够使用现有 SSH 配置正常登录远程服务器。

用户不需要熟悉 SSH 端口转发或 Linux 代理配置。

## 4. 产品目标

首版需要实现以下结果：

- 用户不需要手工修改 SSH 配置或远程代理配置。
- 工具能够判断当前配置缺在哪一步，而不是盲目重复写入。
- 工具完成配置后，能够证明远程服务器已经可以通过本地代理访问 OpenAI。
- 如果网络已经恢复但 Codex 仍处于旧的失败会话，工具能明确要求用户重连或重载。
- 操作失败时，用户能看到具体失败环节和下一步行动。

## 5. 首版核心场景

### 5.1 首次配置

用户可以正常 SSH 登录远端，但从未配置过代理转发。工具发现本地代理，询问或选择远程代理端口，完成必要配置，并引导用户重新连接和验证。

### 5.2 修复不完整配置

用户已经配置了部分内容，但存在端口不一致、缺少转发、远程配置未生效或旧会话未重启等问题。工具指出具体问题，并只修复必要部分。

### 5.3 检查已有配置

用户的配置已经正确。工具不重复修改文件，只执行验证并报告当前状态。

## 6. 核心使用流程

1. 用户启动工具。
2. 用户选择一个已有的 SSH Host。
3. 工具检测本地代理；无法可靠检测时，请用户确认代理地址和端口。
4. 工具检查本地代理、SSH 配置和远程环境。
5. 工具用易懂的语言展示将要进行的修改，并请求确认。
6. 工具完成本地 SSH 转发和必要的远程 Codex 代理配置。
7. 工具提示或协助用户重新建立 VS Code Remote SSH 会话。
8. 工具验证远程代理是否能够访问 OpenAI。
9. 工具给出“可以使用”“需要重连”或“配置失败”的明确结果。

在正常路径下，用户只需选择目标主机、确认代理信息并完成必要的 SSH/VS Code 交互，不需要打开配置文件或执行排障命令。

## 7. 功能需求

### P0：首版必须具备

#### 7.1 选择 SSH 主机

- 读取用户已有的 SSH 配置并列出可选择的 Host。
- 不负责创建 SSH 账号、密码、密钥或主机信息。
- 在修改前确认所选 Host 可以正常连接。

#### 7.2 获取本地代理

- 检查用户提供或工具检测到的本地 HTTP 代理是否正在监听。
- 向用户清楚展示最终使用的本地地址和端口。
- 不把教程中的示例端口当作用户的真实端口。

#### 7.3 检查现有状态

- 检查目标 Host 是否已有相同或冲突的 SSH 反向转发。
- 检查远端是否已经存在可用的代理入口。
- 检查远程 Codex 所在环境是否已经使用该代理。
- 对“未配置”“部分配置”“已经配置”“存在冲突”给出不同结果。

#### 7.4 自动配置

- 为目标 SSH Host 添加或修正所需的反向端口转发。
- 配置远程 Codex 运行环境，使其使用远端回环地址上的代理入口。
- 重复执行不会产生重复配置。
- 只修改完成当前目标所必需的内容。
- 修改前展示变更并要求用户确认。

#### 7.5 生效引导

- 明确告诉用户：已有 SSH 会话不会自动获得新转发。
- 配置完成后，引导用户重新连接 Remote SSH 或重新加载远程窗口。
- 如果工具不能安全地自动完成 UI 操作，应提供准确、简短的人工操作提示。

#### 7.6 分层验证

工具至少分别报告以下状态：

1. 本地代理是否可用；
2. SSH 配置是否正确；
3. 当前远程代理端口是否可用；
4. 远端能否通过该代理到达 OpenAI；
5. Codex 是否还需要重新连接或重新加载。

OpenAI 返回可识别的 HTTP 响应即可证明网络路径可达；未携带凭证的测试返回 `401` 不应被误判为网络失败。

#### 7.7 失败说明

- 失败结果必须指出失败发生在哪一步。
- 给出用户下一步可以执行的动作。
- 不用笼统的“配置失败”代替具体原因。

#### 7.8 安全与恢复

- 不读取、复制、展示或上传 Codex 登录凭证。
- 不记录 SSH 密码、Token、Cookie 或 Authorization 信息。
- 修改配置前保留可恢复的原始状态。
- 发生失败时，不留下明显重复、残缺或无法识别的配置。
- 发现与用户现有配置冲突时，先停止并说明，不静默覆盖。

### P1：Mac 版本稳定后再考虑

- 提供 VS Code 图形界面和状态面板。
- 自动识别更多本地代理软件。
- 生成经过脱敏的诊断报告。
- 更自动化地完成 Remote SSH 重连或窗口重载。

## 8. 首版不做

- 不安装、启动或管理 Clash Verge 等本地代理软件。
- 不帮助用户购买代理、配置代理规则或解决代理服务本身的问题。
- 不管理 SSH 密码、密钥、`known_hosts` 或服务器账号。
- 不复制或同步 `~/.codex/auth.json`。
- 不支持 Windows 或 Linux 本地电脑。
- 不支持远程 Windows 服务器。
- 不同时支持 Claude Code 或其他 AI 编程工具。
- 不解决多台本地电脑同时争用同一个远程端口的问题；检测到冲突时停止并提示。
- 不通过模拟鼠标和键盘操作 VS Code 作为核心能力。
- 不把产品做成通用 SSH 管理器、通用代理客户端或通用远程开发平台。

## 9. 结果状态

工具最终只需要向用户表达三类主要结果：

### 可以使用

本地代理、SSH 转发和远程 OpenAI 网络验证均通过，当前会话已经具备使用条件。

### 需要重连

配置和网络验证已经通过，但当前 VS Code/Codex 仍可能使用旧会话。用户完成提示的重连或重载后再次验证。

### 配置失败

某个必要环节未通过。工具指出失败环节、未完成的修改以及建议动作。

## 10. 验收标准

首版达到以下条件即可认为产品目标成立：

- 对一个只有正常 SSH 连接、尚未配置代理的目标，用户无需手工编辑文件即可完成配置。
- 本地代理端口和远端代理端口不同时，工具仍能生成正确连接。
- 配置已经正确时，重复执行不会新增重复内容。
- 本地代理未启动、SSH 无法连接或远端端口冲突时，工具不会继续盲目修改。
- 修改配置后，工具明确处理“旧 SSH 会话尚未生效”的情况。
- 远端通过代理访问 OpenAI 并返回 `401` 时，网络检查判定为通过。
- 所有正常输出和错误输出均不包含用户凭证。

## 11. 待确认事项

以下问题不影响确认产品方向，但需要在进入实现前决定：

1. 首版是否允许自动修改远端配置，还是默认只修改本地并提供远端操作提示？
2. 首个可用形态是命令行程序、可双击运行的引导程序，还是 VS Code 插件？
3. 首版是否要求工具自动发现本地代理，还是允许用户直接输入代理地址作为可靠的基础路径？
