# Windows 远程 Codex：Spec 与实施计划

> 状态：T0–T5 已完成，M2 首版验收已通过。依据：[PRD](PRD.md)、[技术架构](TECHNICAL_ARCHITECTURE.md)、[安全边界](SECURITY.md)。
> 完成当前任务的检查后再进入下一项；执行状态以 `docs/E2E_RUN.md` 的实际证据为准。未勾选项不代表现有代码完全不存在。

**目标：** 在一个真实 Windows → Linux → VS Code 远程 Codex 环境中，通过脚本配置本地代理转发，证明实际 Codex 请求经该代理成功，并能安全撤销配置。

**架构：** 一个本地 Node.js 脚本调用系统 OpenSSH，修改目标 SSH 块和两个远端 VS Code 设置；用户重连后，由 VS Code SSH 会话承载转发。独立检查连接不创建转发。

**技术栈：** JavaScript ESM、Node.js 内置测试及标准库、系统 ssh/scp/curl、固定 POSIX sh 操作；唯一运行依赖为 `jsonc-parser`。

本文确定执行顺序与行为，不扩展 PRD。需求冲突以 PRD 为准；遇到目标环境不符合架构假设时记录阻塞，先修订方案，不自行增加备用架构。

## 1. 交付范围与两个里程碑

| 里程碑 | 完成条件 |
| --- | --- |
| M1：首次真实 E2E 通过 | T0–T4 完成：配置、重连、独立网络检查、实际 Codex 进程验证、用户请求与代理路径证据、重复配置及移除均通过 |
| M2：首版验收完成 | M1 成立且 T5 的 PRD 失败用例、恢复说明与运行说明齐全；仅承诺实测支持范围 |

执行顺序：**T0 样本真实试验 → T1–T3 整理已验证操作 → T4 命令级 E2E 复验 → T5 失败验收**。T0 必须先取得运行证据并撤销试验配置，不能只读源码后跳到完整工具实现；必要的预览、恢复信息和写入保护仍须先具备。

固定约束：Windows 本地、受信任 Linux 远端、VS Code Remote SSH 中的 Codex；无认证回环 HTTP 代理；每次一个目标；中文交互。其他平台、工具、后台保活、自动操作 VS Code、历史事务、稳定机器协议及打包发布均不做。

## 2. 命令行为

以下入口已有 T0 试验实现，但尚未完成真实 E2E 验证；本文描述修订后的预期行为。端口为示例，实际值由用户输入。

```powershell
node scripts/e2e.mjs configure --host dev-linux --local-proxy http://127.0.0.1:7890 --remote-port 17890
node scripts/e2e.mjs verify --host dev-linux
node scripts/e2e.mjs remove --host dev-linux
```

| 命令 | 必须发生 | 禁止发生 |
| --- | --- | --- |
| configure | 检查全部前提 → 展示两处增量及远端影响范围 → 确认 → 保存恢复信息 → 写入、复读 → 提示重连 | 未确认写入、自动覆盖冲突、把写入成功报成 Codex 可用 |
| verify | 从本次记录读取端口与路径，独立检查网络及实际后端环境，展示分层结果 | 修改配置、创建转发、发模型请求、自动声明人工验收通过 |
| remove | 预览本次拥有的改动 → 确认 → 比对当前状态 → 逆序恢复 → 复读并提示重连 | 删除用户等价配置、覆盖用户后续修改、停止用户进程 |

- configure 缺少 Host 或本地代理时交互询问；远端端口默认 `17890`，冲突时停止，不自动换端口。非交互环境缺参数或需确认时停止，不增加 `--yes` 绕过。
- configure/verify/remove 均必须支持现有密码登录，由 OpenSSH 直接提示和接收输入。不要求公钥认证，不修改服务器密码、authorized_keys 或 sshd 配置，不使用密码参数、环境变量、AskPass 或脚本代填。不同 SSH/SCP 连接可分别要求密码；只合并同阶段只读检查，不承诺一次输入。没有交互终端且需要认证时停止，提示在 PowerShell 中运行，不以 BatchMode 排除密码用户。
- 端口检查区分首次新增与重复执行：首次新增要求空闲；重复执行先核对本次记录、有效映射和当前配置，匹配且回环端点可用则无修改返回。配置一致但无监听时提示重连；来源不明、映射不符或端点检查失败则报告问题，不改写。已有等价用户配置按相同映射与端点规则复用，始终不接管所有权。
- `--ssh-config` 可指定 VS Code 实际使用的绝对路径；默认用户 `.ssh/config`。必须先确认两者一致。
- `--remote-settings` 指定通过目标 VS Code 窗口确认的实际 Remote settings 文件绝对路径；首次运行未提供时询问，不猜测默认路径。路径定位不需要用户编辑配置。
- verify/remove 复用本次记录；Host 或显式配置路径与记录不符时停止。无记录时说明“没有本次配置记录”，不扫描和猜测所有权。
- Host 校验为 `^[A-Za-z0-9][A-Za-z0-9._-]{0,254}$`，端口为整数 `1024–65535`。代理仅接受 `http://127.0.0.1:端口` 或 `http://localhost:端口`，允许末尾 `/`，拒绝认证信息、其他路径、查询与片段；localhost 归一为 127.0.0.1 并实际探测。
- 命令退出码只用 `0` 表示该命令的职责完成、`1` 表示失败或证据不足；确认被拒绝则正常退出且无修改。configure 的 `0` 仍需重连；verify 的 `0` 只表示自动检查通过，真实请求由人工验收记录确认。

## 3. 文件职责与最小内部接口

已有 T0 脚本、包文件和运行记录继续沿用；下一轮先完成第 4 节的认证探针，再修改试验脚本。下列文件划分与接口在试验通过后整理，不作为 T0 的前置工作。

| 文件 | 职责 |
| --- | --- |
| `package.json`、`package-lock.json` | ESM、唯一运行依赖及锁定版本；`npm test` 调用 `node --test` |
| `scripts/e2e.mjs` | 参数、询问、预览、三个命令的顺序编排、中文结果 |
| `scripts/ssh.mjs` | ssh/scp/curl 参数、终端交互、超时、固定远端操作、过滤后检查证据 |
| `scripts/config.mjs` | SSH 与 JSONC 局部编辑、摘要、文件写入保护、本次记录及恢复 |
| `tests/transport.test.mjs` | 参数隔离、代理与 TLS 判定、超时、输出过滤 |
| `tests/config.test.mjs` | 重复配置、冲突、内容保留、恢复、摘要不符 |
| `tests/workflow.test.mjs` | configure/verify/remove 可见行为及故障注入 |
| `docs/E2E_RUN.md` | 同一份实测清单：环境版本、步骤、证据摘要、未通过原因 |
| `README.md` | 安装运行、重连、移除及当前实测支持范围 |

为便于连接任务，内部普通函数先约定以下边界；它们不是稳定公共 API，禁止因此增加类层级或插件机制：

```js
// ssh.mjs
runProcess({ executable, args, timeoutMs, stdio })
// Promise<{ exitCode: number | null, stdout: string, stderr: string, timedOut: boolean }>
inspectTarget(options)
// Promise<{ ok: boolean, checks: Check[], snapshot: object | null }>
verifyTarget(options, record)
// Promise<{ ok: boolean, checks: Check[] }>

// config.mjs：生成不写入的编辑结果
prepareEdits({ sshText, settingsText, host, localPort, remotePort })
// { sshText: string, settingsText: string, changed: boolean, recovery: object }
// settingsText 输入为 null 表示原文件不存在；冲突抛出不含秘密的错误。

// e2e.mjs：供命令入口与行为测试共用
runCommand({ command, options, io, dependencies })
// Promise<number>；依赖参数仅用于测试替换进程/文件操作，不暴露给用户。

// Check = { layer: string, status: 'pass' | 'fail' | 'unknown', message: string }
// layer 仅使用 local-proxy、ssh、remote-endpoint、target-https、codex-process。
```

SSH 候选通道为 `stdio: ['inherit', 'pipe', 'inherit']`：stdin/stderr 交给 OpenSSH 与终端，stdout 仅供脚本解析。继承 stderr 时结果中的 stderr 为空，不从中猜测密码提示或承诺诊断精度；OpenSSH 原生提示直接显示但不由工具捕获、持久化。远端操作的预期错误通过 stdout 返回不含秘密的状态码，不输出配置正文到终端。curl 等无需交互的本地调用保留管道，不能全局切换所有子进程。`snapshot` 是内存中的配置与观察结果，不能直接序列化为报告。

## 4. T0：先用最小脚本验证一套真实环境

**依赖：** 无。**文件：** 最小 `package.json` / 锁文件、`scripts/e2e.mjs` 试验脚本、`docs/E2E_RUN.md`。**产出：** 样本路径可行或阻塞的运行证据；完整 CLI 尚未实现。

### T0 前置：先验证密码交互与文件传输

现有证据：直接 PowerShell SSH 密码登录成功，Node 管道调用没有提示并超时。终端继承是待验证方案，当前不能宣称已解决。下面三步依次通过后才继续配置试验，不先重构完整 CLI。

- [x] 在真实 PowerShell 中，用同一个 ssh.exe、配置文件、Host 和禁用转发/复用参数作对照：直接 SSH 执行固定成功标记；最小 Node 探针先继承三个终端通道执行同一命令，再仅把 stdout 改为 pipe。探针不读写用户配置。确认密码提示可见、输入不回显、Node 能解析完整标记且退出码为 0。只测本地进程替身不算通过；失败则停在该步骤，记录具体组合，不要求用户改认证方式。
- [x] 验证取消认证或认证失败能结束且不进入远端操作。认证等待与远端执行分开计时：认证等待上限暂定 120 秒并预先提示，收到远端就绪标记后开始操作超时；就绪标记只证明已开始执行，最终成功还需完整结果和退出码 0。不得由脚本读取密码或伪造密码输入提示。
- [x] 使用同一套系统 OpenSSH 的 scp.exe，保持同一配置入口、认证与主机密钥检查，禁用转发和复用。先确认目标支持该客户端默认文件传输方式（通常为 SFTP），不自动切换协议或安装服务。仅在私有临时目录传输无敏感内容样本，验证摘要一致、空格路径、退出结果及清理；上传过程保留原生终端认证。此步不修改两处用户配置。

### T0 后续：沿用最小配置闭环

- [x] 向用户取得实际 SSH alias、本地代理端口与本次测试目标；记录 Windows、Node、ssh、VS Code、Remote SSH、Linux、远端 Codex 扩展版本。私有地址、用户名和完整路径只在本地记录中保存，仓库报告使用代称。
- [x] 用 VS Code 的 Running Extensions 确认 Codex 在远端；用 Open Remote Settings (JSON) 确认设置文件路径，核对 VS Code 的 SSH 可执行程序与配置入口。
- [x] 只读检查远端已安装包中“读取 http.proxy → 设置子进程环境 → 启动 app-server”的实际调用链，记录版本和证据位置。不得把 Windows 安装包证据直接当作 Linux 证据。
- [x] 确认远端具备 sh、curl、监听检查及试验实际所需的文件操作能力；选择已有工具，不自动安装。确认两个配置位置无冲突；首次新增要求端口空闲，已有配置按第 2 节分支核对。
- [x] 写针对该样本的最小预览、应用和撤销脚本：保存目标字段原值/缺失、插入片段与摘要，限制恢复信息访问；同目录临时文件替换前核对摘要，失败保留证据。先在临时文件上确认能撤销且不会覆盖后续改动，不依赖 T1–T3 的完整接口、交互或测试套件。
- [x] 展示两处增量及影响范围，用户确认后应用。用户重连，维护者运行禁用转发与复用的独立检查，确认回环绑定、目标 HTTPS 可达和新 Codex 后端代理字段匹配。
- [x] 结束主动网络探测，由用户发送简短请求；按技术架构第 6 节关联实际 Codex 后端进程与远端代理端口连接，记录新回复。单纯同时段代理日志不能作为试验通过依据。
- [x] 再次生成增量应为空，保留最初恢复信息；撤销试验改动并由用户重连，确认原配置和普通 SSH 保留。证据不足或撤销未完成则记录阻塞，不进入 T1–T3 的完整封装。

**完成判据：** 实际进程采用代理、用户请求经该路径成功、试验改动已安全撤销；仅静态调用链或 curl 成功不算通过。T0 证明路径可行，T4 再验收整理后的完整命令，避免先实现工具再发现核心假设不成立。

## 5. T1：实现只读连接与网络检查

**文件：** 沿用 T0 包文件，从试验脚本整理 `scripts/ssh.mjs`，创建 `tests/transport.test.mjs`；记录实际 Node 版本，保留锁定的依赖。**接口：** 产出 `runProcess`、`inspectTarget`，后续 verify 复用。

- [x] 先写失败测试，覆盖外部进程使用参数数组且 `shell: false`、超时有界结束、捕获的输出只进入分类器而不直接展示；SSH/SCP 原生终端提示按第 3 节处理，不纳入工具日志。
- [x] 整理 T0 已实测的系统 ssh/scp/curl 调用及终端模式。SSH stdin 仅用于原生交互，不再传送脚本或配置数据。短小固定脚本及经校验的非秘密参数编码后放入远端命令，使用固定解码执行模板；远端 Shell 边界仍须验证，Base64 不等于加密。完整配置内容只通过捕获的 stdout 读取、SCP 文件上传，不进入命令参数或日志。
- [x] 所有 SSH/SCP 调用统一使用同一目标配置入口和主机密钥检查，不关闭检查；先确认终端输入未被 Node 询问器占用，再交给子进程。认证等待、执行超时与 Ctrl+C 均须结束本次子进程；中断写入时沿用恢复规则。
- [x] 所有读取、写入和检查连接都清除自动转发并禁止连接复用，避免本地 SSH 配置写入后，后续设置操作偷偷建立转发。只有用户的 VS Code 连接承载实际转发。
- [x] 本地及远端 curl 沿用架构的固定目标、TLS、代理和超时策略；分类 curl 退出码、CONNECT 代理状态、最终目标状态。传输/TLS 失败、代理 407 不通过；目标 401 只表示该目标网络可达。
- [x] 整理目标配置只读获取、首次/重复配置的端口检查与分层摘要；文件内容不打印。未知 SSH 错误只给脱敏类别和下一步，不凭字符串猜成认证或网络成功。
- [x] 运行 `node --test tests/transport.test.mjs`，确认通过后，在 T0 样本上运行一次只读基线检查，记录结果。

必须包含的测试数据：

```text
curl 正常退出 + CONNECT 200 + 目标 401 + TLS 校验成功 → 网络检查通过
CONNECT 407                                  → 代理失败
curl TLS 错误 / 超时 / 连接拒绝                 → 检查失败
非交互调用捕获的 stderr 含测试用 Token           → 工具输出及持久记录不含该值
检查 SSH 参数                                 → 含 ClearAllForwardings=yes，且不含 -R
```

**完成判据：** 在不改任何配置的情况下，能区分本地代理、SSH 与目标网络问题；检查命令退出后无遗留子进程。

## 6. T2：实现可预览、可撤销的最小配置编辑

**文件：** 从 T0 试验脚本整理 `scripts/config.mjs`，创建 `tests/config.test.mjs`。**接口：** 产出 `prepareEdits` 和模块内部写入/恢复函数，T3 调用；沿用已实测的两处编辑，不另建实现。

- [x] 先写 SSH 片段与 JSONC 编辑的失败测试。覆盖唯一字面 Host 块、注释/换行保留、两个端口不同、二次执行不变，以及等价用户值不归本工具所有。
- [x] 实现原文本上的局部编辑。拒绝 Include、Match、重复目标块、链接文件、不完整工具标记、非法 JSONC、重复目标键及不同用户代理值。不要用正则替换整个 JSON，也不要整体序列化设置文件。
- [x] 在临时文件上实现保存本次恢复信息、摘要比对、同目录临时文件、权限保留、替换后复读。远端写入采用：本地受限临时文件 → SCP 上传至远端私有暂存目录 → 固定 SSH 脚本检查上传摘要与原文件摘要 → 同目录临时文件替换 → 复读。远端目录权限 0700、暂存文件 0600；替换用户文件时保留原权限。预先检查所需工具与传输能力；替换前失败不改原文件，替换结果未知则保留恢复记录并停止。
- [x] 恢复信息仅保存目标字段原值/缺失、插入片段、原文件是否存在和摘要。恢复前检查状态未被用户改动；全文件有变化也可保守停止，不为首轮引入智能合并。
- [x] 覆盖本地状态目录访问限制、创建失败时不改用户文件、相关原值含秘密时拒绝。私有临时文件完成或失败后清理；不得留下完整设置副本作为长期备份。
- [x] 运行 `node --test tests/config.test.mjs`；整理后的代码再次写入真实文件前，确认“远端写失败后恢复本地”和“当前摘要不同则不覆盖”两个测试通过。

最小行为测试示例（测试真实编辑函数，不断言内部调用次数）：

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { prepareEdits } from '../scripts/config.mjs';

test('重复配置不新增内容，保留用户设置', () => {
  const input = {
    sshText: 'Host dev-linux\n    HostName example.invalid\n',
    settingsText: '{\n  // 保留注释\n  "editor.tabSize": 2\n}\n',
    host: 'dev-linux', localPort: 7890, remotePort: 17890,
  };
  const first = prepareEdits(input);
  const second = prepareEdits({
    ...input, sshText: first.sshText, settingsText: first.settingsText,
  });
  assert.equal(second.changed, false);
  assert.equal(second.sshText, first.sshText);
  assert.equal(second.settingsText, first.settingsText);
  assert.match(first.settingsText, /\/\/ 保留注释/);
  assert.match(first.settingsText, /"editor.tabSize": 2/);
  assert.match(first.sshText, /RemoteForward 127\.0\.0\.1:17890 127\.0\.0\.1:7890/);
});
```

**完成判据：** 预览无写入；配置可重复、可撤销；恢复不会覆盖新修改。此任务不能只完成写入而把移除留到实测之后。

## 7. T3：接通 configure 与 remove

**文件：** 将 `scripts/e2e.mjs` 试验入口整理为三个命令，创建 `tests/workflow.test.mjs`，更新 README 运行说明。**接口：** `runCommand` 消费 T1、T2 的能力。

- [x] 先测试拒绝确认时文件完全不变，以及依赖检查失败时不进入写入。
- [x] 实现参数校验、交互补齐和一屏预览：目标代称、两端端口、两处改动、同一远端其他扩展可能受影响、恢复位置。不得展示整份配置。
- [x] 单次 configure 顺序固定：完整检查 → 准备两处编辑 → 确认 → 获取单实例写入保护 → 重新核对摘要 → 保存恢复信息 → SSH 配置写入 → `ssh -G` 差异检查 → 远端设置写入 → 复读 → 输出重连步骤。
- [x] 重复 configure 在写入流程前走第 2 节分支：已有转发正常占用不报冲突；无增量则保持原文件与首次恢复记录不变。不要用已应用值覆盖最初原值，也不要仅因端点可用就认领其所有权。
- [x] 应用中任一步失败，逆序恢复已改内容并复读。Ctrl+C 或远端连接中断时保留恢复记录；结果无法确认则报“需要恢复”，禁止继续覆盖。
- [x] 实现 remove：核对记录和当前摘要，先恢复远端设置再移除本地工具片段；原本等价的用户值保留。新建 settings 文件只在无其他内容时删除。
- [x] 一次仅有一个未移除的目标记录。重复相同 configure 是检查/无改动；未解决的记录或另一目标阻止新写入。remove 能使用旧记录恢复，不依赖代理当前可用。完成移除才允许开始下一次配置，不增加历史查询系统。
- [x] 运行 `node --test tests/workflow.test.mjs`，通过“确认拒绝、配置需重连、已知端口占用下重复配置、来源不明占用停止、重复后仍能正确移除、远端写失败恢复”后再允许 T4 的真实复验。

**完成判据：** 两个命令在临时环境中形成配置与撤销闭环；命令行正常结果与恢复不确定结果区分明确。verify 尚未通过时不得输出 E2E 成功。

## 8. T4：封装 verify 并完成命令级真实 E2E 复验

**文件：** 更新 `scripts/ssh.mjs`、`scripts/e2e.mjs`、`tests/transport.test.mjs`、`tests/workflow.test.mjs`、`docs/E2E_RUN.md`。**接口：** 实现 `verifyTarget`，由 verify 命令调用。

- [x] 先写测试：verify 不修改配置、不请求转发；网络通过而实际进程证据缺失时返回证据不足。
- [x] 实现独立探测及实际监听地址检查。连接到 127.0.0.1 成功不替代绑定地址检查；发现 wildcard 监听停止并提示用户断开本次连接。
- [x] 定位该目标窗口的远端 app-server。若多个候选无法关联，要求维护者确认 PID，不任选第一个；只读检查进程归属与代理字段，在远端过滤为匹配/冲突/缺失后返回。
- [x] 核对大写 HTTP_PROXY / HTTPS_PROXY、冲突小写值及 NO_PROXY/no_proxy 绕过；没有证据时使用 unknown，不能仅从 settings.json 的值推断运行状态。
- [x] 运行相关测试，通过后执行真实 configure。用户保存工作、关闭目标远端窗口、用同一 alias 重连并打开 Codex；确认后端已重启，禁止默认 Kill Server。
- [x] 执行 verify，记录五层结果并结束主动网络探测；用户发起简短请求，按架构第 6 节取得目标 Codex 后端进程到远端代理端口的关联证据及新回复。只有同时段/同域名的代理日志时记录“归因未证实”，不判 M1 通过。脚本不代发模型请求。
- [x] 再次 configure，确认内容无变化；remove 后由用户重连，确认工具配置已撤销、普通 SSH 可用。需要继续使用时重新配置，再验证。
- [x] 在 `docs/E2E_RUN.md` 标明 M1 通过或具体失败步骤；缺少代理路径证据只能记录“可用但归因未证实”。

**完成判据：** T0 样本有完整的配置、运行、请求归因及撤销证据，才可标记 M1。自动测试不能替代这一步。

## 9. T5：补齐 PRD 失败验收与交付说明

**文件：** 三个测试文件、README、`docs/E2E_RUN.md`；只修复用例暴露的问题。

| 用例 | 验证方式 | 预期 |
| --- | --- | --- |
| 本地代理未启动 | 用测试夹具提供的未监听本地端口 | 检查失败，两处用户文件不变 |
| SSH 失败 | 进程替身返回认证/连接失败，保留一个可复现样例 | 不写入、不泄露错误中的秘密，下一步清楚 |
| 密码交互 | T0 真实 PowerShell 登录及取消记录；自动测试补充退出/超时分支 | 原生密码提示可用；失败或取消不继续写入，模拟测试不能替代实测 |
| SCP 失败 | 临时样本验证传输不可用、上传中断或摘要不符 | 不替换目标配置；清理临时文件，无法清理时列出本次残留路径 |
| 远端端口冲突 | 临时测试环境的受控占用；真实目标已有自然冲突时可补实证 | 不切换端口、不杀进程、不修改配置 |
| 两端端口不同 | T4 使用不同端口 | 真正请求成功且转发映射正确 |
| 配置重复与等价用户配置 | 文本夹具及 T4 二次执行 | 不新增内容，remove 不删除用户所有内容 |
| 已知转发占用与无监听 | 工作流夹具及 T4 重连后二次配置 | 已知映射且端点可用时无修改通过；配置一致但无监听时提示重连，首次恢复信息不变 |
| 代理日志来自其他请求 | 验收夹具只有相同时间、域名的日志，没有目标进程关联 | 归因未证实，不计入 M1 |
| 本地写入失败 | 文件操作故障注入 | 远端不被修改，原文件保留 |
| 远端写入失败/回复丢失 | 固定操作边界故障注入 | 本地可恢复；远端状态未知则保留证据，重读后判定，不谎称恢复 |
| 用户修改后再移除 | 配置后修改夹具中的原文件 | 摘要不符时停止，用户修改保留 |
| 秘密与日志 | 错误及无关配置中放测试 Token | 对外输出和恢复记录均无 Token、完整配置或原始环境 |

- [x] 每个行为先建立失败测试，实施最小修复，再运行对应测试；不新增测试框架或公共协议。
- [x] 执行 `npm test` 和 `git diff --check`。真实凭证不进入测试夹具，不为故障注入停止用户代理、断网或破坏真实配置。
- [x] README 写明实际 Node 版本要求、`npm ci`、三个命令、参数、重连步骤、移除失败的记录位置和人工核对步骤，并标明 Include/Match 等首轮限制。
- [x] 实测记录逐项标注“真实环境通过 / 自动测试通过 / 未执行 / 阻塞”，不能混写。M2 只有 PRD 对应项均满足时才能标记完成。

## 10. 实测记录格式与执行边界

`docs/E2E_RUN.md` 在实施时使用以下固定栏目；尚未运行的项目写“未执行”，不填假数据：

```text
执行日期与版本组合
测试目标代称、两个配置位置已确认（是/否）、两端端口
密码交互：直接 SSH / Node 全继承 / Node 捕获 stdout / 取消结果；SCP 样本传输与清理结果
远端扩展运行位置与目标包静态证据
T0 样本试验：进程采用代理、请求路径证据及撤销结果
本地代理 / SSH / 远端入口 / 目标 HTTPS / Codex 进程：各自结果
用户新请求：成功或失败；主动探测已结束（是/否）；代理路径：目标进程关联证据或归因未证实
重复配置与移除结果
失败用例：验证方式、结果、尚未覆盖项
M1 / M2：未通过或通过，并附依据
```

本计划的文档交付不触发真实写入、模型请求、提交或推送。后续执行真实验证时，取得具体目标并遵循脚本变更确认；重连和模型请求由用户完成。每个任务完成后只更新相应检查项与证据，失败时停在当前阻塞，不把范围扩展成跨平台产品工程。
