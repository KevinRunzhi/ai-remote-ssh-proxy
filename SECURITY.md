# 安全边界

项目尚无可运行版本，首版范围以 [PRD.md](PRD.md) 为准。

- 信任本地用户、操作系统及目标 Linux 环境；不支持不受信任的共享服务器，远端回环端口仍可被同机其他用户和进程访问。
- 本地仅使用回环地址上的无认证 HTTP 代理，远端入口仅绑定 127.0.0.1。
- SSH 认证和主机密钥检查交给 OpenSSH，不关闭检查，不管理密码、私钥或 Codex 登录凭证。
- 仅修改展示并经用户确认的配置；保留必要恢复信息，限制访问权限，不记录秘密。
- 冲突、用户后续修改或无法确认安全的文件状态应停止；无法确认恢复时说明残留和人工恢复步骤。
- 不自动上传诊断数据，不在公开 Issue 提供凭证或私有主机信息。首次公开发布前确定私密漏洞报告渠道。
